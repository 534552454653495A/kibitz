Fixes #{{ISSUE}}

Automated selector-contract repair — **ai-fix round {{ROUND}}** (max 5 per issue).

## What the agent found and changed

{{RATIONALE}}

## Evidence

- Probe run this fix is based on: {{EVIDENCE_RUN_URL}}
- Producing workflow run: {{FIX_RUN_URL}}

## Process

- Generated by the Kibitz fix agent (`.github/workflows/ai-fix.yml`) under the rules in
  AGENTS.md 7.1: allowlisted paths only, diff-size cap, `npm run check` green, no GitHub
  access for the agent.
- Reviewed by `ai-review` (job summary + `review.json` artefact); the canary probe runs on
  this PR for Stable and Canary.
- **Merge requires a human.** Nothing in this pipeline can merge.
