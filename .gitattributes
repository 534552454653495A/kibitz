# LF everywhere, on every OS. The .github/scripts/*.sh files run under bash on Linux
# runners (and under WSL locally); a CRLF checkout on Windows would turn their shebangs
# into "bash\r" and every script into "command not found".
* text=auto eol=lf
*.png binary
